    <nav class="navbar navbar-expand-md bg-body-tertiary border-bottom sticky-top">
        <div class="container-fluid">
            <a class="navbar-brand" href="/listings">
                <i class="fa-brands fa-joomla"></i>
            </a>
            
            <!-- Hamburger Button -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" 
                aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <!-- Collapsible Navbar Content -->
            <div class="collapse navbar-collapse mt-3 mt-md-0" id="navbarNavAltMarkup">
                <div class="gap-2">
                    <a class="nav-link" href="/listings">All Listings</a>
                </div>
                <div class="gap-2 ml-2">
                    <% if(currUser) { %>
                        <a class="nav-link" href="/listings/your-listings">View Your Listings</a>
                    <% } %>
                </div>
                <div class="ms-auto">
                    <form class="d-flex" role="search" action="/listings/search" method="GET">
                        <input class="form-control me-2 search-bar" type="search" name="searchText" 
                            id="searchBar" placeholder="Search Destination" aria-label="Search" autocomplete="new-password"/>
                        <button class="btn btn-success btn-search" type="submit">
                            <i class="fa-solid fa-magnifying-glass"></i>Search
                        </button>
                    </form>
                </div>
                <div class="navbar-nav ms-auto gap-2">
                    <a class="nav-link" href="/listings/createNew">Add New Listing</a>
                    <% if (!currUser) { %>
                        <a class="nav-link" href="/signup"><b>Sign Up</b></a>
                        <a class="nav-link" href="/login"><b>Login</b></a>
                    <% } %>
                    <% if (currUser) { %>
                        <a class="nav-link" onclick="return confirm('Are you sure you want to log out?')"href="/logout"><b>Logout</b></a>
                    <% } %>
                </div>
            </div>
        </div> 
    </nav>


<!-- This is Navbar for the entire website -->